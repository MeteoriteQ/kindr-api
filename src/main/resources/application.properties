spring.application.name=Kindr-Complete-Integrated
server.port=8080

# One database for every module
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/kindr?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# Forgot-password email. Set environment variables before testing email OTP.
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=konda.yanaki@gmail.com
spring.mail.password=djsvapxzpogxdyzh
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
notification.email.from=konda.yanaki@gmail.com
app.frontend-url=${APP_FRONTEND_URL:http://localhost:5173}

# Razorpay. If keys are empty, /api/payments/config returns test mode.
razorpay.key.id=rzp_live_TYhoLFHJ7jIUpS
razorpay.key.secret=ptnEyo0n66ReccCX0L3ACv2C

# Maximum allowed size for a single file
spring.servlet.multipart.max-file-size=500MB

# Maximum allowed size for an entire multipart request
spring.servlet.multipart.max-request-size=500MB

# Prevents Tomcat from abruptly cutting off the connection for large files
server.tomcat.max-swallow-size=-1
